<%- include('./header'); %>
<form>

    Cliente:
    <select id=idcliente>
        <%
		var p = pessoas.length;
		for(var i=0;i<p;i++)
		{
	    %>
            <option><%=pessoas[i].c.nome%></option>>
        <% } %>
        
    </select>

    Produto:
    <select id=idproduto>
        <%
		var p = pessoas.length;
		for(var i=0;i<p;i++)
		{
	    %>
            <option><%=pessoas[i].nome%></option>>
        <% } %>
        
    </select>

</form>